/* eslint-env jest, browser */
import videoControls from '../src';

test('videoControls is implemented', () => {
  expect(() => videoControls()).not.toThrow();
  throw new Error('implement videoControls and write some tests!');
});
